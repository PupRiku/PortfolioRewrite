'use strict';

/**
 * additional-experience service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::additional-experience.additional-experience');
